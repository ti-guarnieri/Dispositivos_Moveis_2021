<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timi.dispositivosmoveis2021">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DispositivosMoveis2021">
        <activity android:name=".detalhe.DetalheTodoActivity"></activity>
        <activity android:name=".detalhe.DetalhePostsActivity" />
        <activity android:name=".detalhe.DetalheCommentsActivity" />
        <activity android:name=".ui.CommentsActivity" />
        <activity android:name=".ui.TodoActivity" />
        <activity android:name=".ui.PostsActivity" />
        <activity android:name=".ui.DetalhePhotoActivity" />
        <activity android:name=".model.activity_photos" />
        <activity android:name=".ui.PhotoActivity" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>